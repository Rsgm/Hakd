package hakd.game.gameplay;

import hakd.networks.Network;

public class Employee extends Character {
    public Employee(Network network, String name, City city) {
        super(network, name, city);
    }
}
