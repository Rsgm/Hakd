package hakd.game.gameplay;

import hakd.networks.Network;

public class Company extends Character {
    public Company(Network network, String name, City city) {
        super(network, name, city);
    }
}
