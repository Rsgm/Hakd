package hakd.game.gameplay;

import hakd.networks.Network;

public class Hacker extends Character {
    public Hacker(Network network, String name, City city) {
        super(network, name, city);
    }
}
